services:
  gateway:
    build: .
    ports:
      - "${ZEN_GATEWAY_PORT:-8789}:8789"
    volumes:
      - ./data:/data
    env_file:
      - .env
    environment:
      DATA_DIR: /data
    restart: unless-stopped
